<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pesticide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Last_page"></activity>
        <activity android:name=".Information" />
        <activity android:name=".Harvesting" />
        <activity android:name=".Fruiting" />
        <activity android:name=".Flowering" />
        <activity android:name=".Vegetative" />
        <activity android:name=".Seedling" />
        <activity android:name=".Stages" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>